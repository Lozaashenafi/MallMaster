@model List<AppUser>
@{
    ViewData["Title"] = "tenant Page";
    @* Layout = null; *@
}
<div class="row">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title text-warning">Tenant List</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th class="text-white">Tenant Name</th>
                            <th class="text-white">Room NO</th>
                            <th class="text-white">Floor NO</th>
                            <th class="text-white">Phone Number</th>
                            <th class="text-white">Payment Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td class="text-light">@user.FirstName</td>
                                <td class="text-light">012</td>
                                <td class="text-light">2</td>
                                <td class="text-light">0988776555</td>
                                <td>
                                    <div class="badge badge-outline-success">
                                        payed
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
