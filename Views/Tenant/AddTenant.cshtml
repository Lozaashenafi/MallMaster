@{
    ViewData["Title"] = "add tenant Page";
    @* Layout = null; *@
}
<div style="margin: 0 auto;" class="col-md-6 grid-margin stretch-card ">
    <div class="card-body">
        <h4 class="card-title text-warning">Add New Tenant </h4>
        @* <p class="card-description"> Basic form layout </p> *@
        @Html.Partial("~/Views/Account/Register.cshtml")
    </div>
</div>
